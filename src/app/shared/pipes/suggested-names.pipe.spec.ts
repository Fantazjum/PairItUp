import { SuggestedNamesPipe } from './suggested-names.pipe';

describe('SuggestedNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new SuggestedNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
