import { LanguageIconsPipe } from './language-icons.pipe';

describe('LanguageIcons', () => {
  it('create an instance', () => {
    const pipe = new LanguageIconsPipe();
    expect(pipe).toBeTruthy();
  });
});
